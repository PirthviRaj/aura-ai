# Optional — app works without these, but GPT-4o needs OPENAI_API_KEY for public-quality AI

OPENAI_API_KEY=

# Supabase Auth (optional). Enable Google / GitHub / Azure providers in the dashboard.
# Redirect URL: http://localhost:3000/auth/callback  (and your production /auth/callback)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
